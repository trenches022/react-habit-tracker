import './Settings.css';

const Settings = () => {
  return (
    <div className="settings-container">
      <h1>Settings</h1>
      <div className="setting-dark-mode">
        <h3>Dark Mode (In progress...)</h3>
        <button className="theme-switch-btn">Switch Theme</button>
      </div>
    </div>
  )
}

export default Settings;